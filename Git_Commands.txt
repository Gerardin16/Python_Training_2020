git config --global user.email "you@example.com"
git config --global user.name "Your Name"

git init
git init ['Folder Name']
git clone

git status

git add
git add .
git add --all


git commit
git commit .

git branch
git branch [name]
git checkout [name]
git branch -d/D [name]

git remote add orgin https://github.com/loyolastalin/TestingGit.git
git remote
git push orgin master
git pull orgin master
